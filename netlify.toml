[build]
  publish = "dist"
  command = "npm cache clean --force && npm install --no-optional && npm run build"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200 